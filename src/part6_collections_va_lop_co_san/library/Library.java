package part6_collections_va_lop_co_san.library;

/**
 * Class Library - Quản lý thư viện sách
 *
 * Mô tả:
 * - Class này quản lý danh sách các quyển sách trong thư viện
 * - Cần implements interface ILibrary
 * - Sử dụng ArrayList để lưu trữ danh sách sách
 *
 * Thuộc tính:
 * - books: List<IBook> - Danh sách các quyển sách (sử dụng ArrayList)
 *
 * Yêu cầu:
 * 1. Tạo constructor khởi tạo ArrayList rỗng
 * 2. Implement các methods:
 *    - addBook(): Thêm sách vào danh sách
 *    - removeBook(): Xóa sách theo tên (nếu tìm thấy)
 *    - findBookByTitle(): Tìm sách theo tên (trả về null nếu không tìm thấy)
 *    - findBooksByAuthor(): Tìm tất cả sách của một tác giả
 *    - getAllBooks(): Trả về danh sách tất cả sách
 *    - getTotalBooks(): Trả về tổng số sách
 *    - getTotalValue(): Trả về tổng giá trị của tất cả sách
 *    - sortBooksByPrice(): Trả về danh sách sách đã sắp xếp theo giá tăng dần
 * 3. Override toString() để in danh sách sách, mỗi sách trên một dòng
 */
class Library implements ILibrary {
    // TODO: Khai báo thuộc tính

    // TODO: Tạo constructor

    // TODO: Implement các methods từ interface

    // TODO: Override toString()
}

